{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [
            ],
            "initCommands": [
            ],
            "env": {
                // "LANG": "en_CH.UTF-8"
                // "GTK_DEBUG": "interactive",
            },
        }
    ]
}